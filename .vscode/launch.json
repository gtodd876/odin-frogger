{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Odin Program",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/frogger",
            "args": [],
            "cwd": "${workspaceFolder}/src",
            "preLaunchTask": "build-debug"
        }
    ]
}