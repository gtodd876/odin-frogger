{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Odin Program",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/frogger",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build-debug"
        }
    ]
}